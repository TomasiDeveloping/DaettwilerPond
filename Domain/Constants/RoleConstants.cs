namespace Domain.Constants
{
    // Constants representing different user roles within the application
    public static class RoleConstants
    {
        // Role for administrators with elevated privileges
        public const string Administrator = "Administrator";

        // Default role for regular users
        public const string User = "User";

        // Role for overseers with specific responsibilities (German translation: Aufseher)
        public const string Overseer = "Aufseher";
    }
}